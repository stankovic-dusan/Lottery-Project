{
  "name": "lottery",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "npm run clean && npm run compile",
    "compile": "npx hardhat compile",
    "clean": "npx hardhat clean",
    "test": "npm run build && npx hardhat test",
    "test:only": "npx hardhat test",
    "deploy": "npx hardhat run scripts/deploy.ts --network ropsten"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@nomicfoundation/hardhat-chai-matchers": "^1.0.3",
    "@nomicfoundation/hardhat-toolbox": "^2.0.0",
    "@types/mocha": "^9.1.1",
    "@types/node": "^18.7.18",
    "ethers": "^5.7.1",
    "hardhat": "^2.11.2",
    "ts-node": "^10.9.1",
    "typescript": "^4.8.3"
  },
  "dependencies": {
    "dotenv": "^16.0.2"
  }
}
